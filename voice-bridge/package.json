{
  "name": "olma-voice-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Olma on a phone call: Twilio media streams in, Hebrew STT/TTS, the real domain functions",
  "scripts": {
    "test": "node --test 'tests/*.test.js'",
    "check": "node --check server.js && node --check lib/persona.js && node --check lib/env.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
